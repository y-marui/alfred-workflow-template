name: Dev Charter

on:
  schedule:
    - cron: "1 1 * * 3"  # Every Wed 01:01 UTC
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  check:
    uses: y-marui/dev-charter/.github/workflows/check-charter.yml@main
    with:
      prefix: docs/dev-charter
